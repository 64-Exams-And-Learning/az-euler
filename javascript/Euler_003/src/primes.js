(function(){

    // a prime number test test for `x`
    // - if `x` mod `p` == 0 then not prime
    var isPrime = function(testValue){
        var primes = [1],
            isPrime = true,
            i = primes.length;
        do {
i =- 1;
if (i ===0){isPrime = false; break;}
            isPrime = testValue 
        } while (isPrime);
    }
})();